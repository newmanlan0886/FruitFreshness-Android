name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk python3-pip \
            autoconf libtool pkg-config zlib1g-dev libncurses5-dev \
            libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
          pip install --user buildozer cython
      
      - name: Create .env file
        run: |
          if [ -n "${{ secrets.GEMINI_API_KEY }}" ]; then
            echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" > .env
          else
            cp .env.example .env 2>/dev/null || echo "GEMINI_API_KEY=dummy" > .env
          fi
      
      # 🔧 準備自訂 python-for-android 目錄
      - name: 📦 Prepare custom p4a with pyjnius hotfix
        run: |
          P4A_DIR="/home/runner/work/p4a"
          # 確保目錄乾淨
          rm -rf "$P4A_DIR"
          # 克隆 python-for-android（master 分支）
          git clone -b master --single-branch https://github.com/kivy/python-for-android.git "$P4A_DIR"
          
          # 🔥 修復 pyjnius 的 long 型別錯誤（Python 3）
          JNIUS_FILE="$P4A_DIR/pythonforandroid/recipes/pyjnius/patches/fix_long.patch"
          # 直接修改 jnius_utils.pxi（比 patch 更直接）
          sed -i 's/isinstance(arg, long)/isinstance(arg, int)/g' \
            "$P4A_DIR/pythonforandroid/recipes/pyjnius/jnius/jnius_utils.pxi"
          
          echo "✅ pyjnius hotfix applied."
          
      - name: Build with Buildozer
        run: |
          # 不需要任何忽略參數，直接建置
          buildozer android debug
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: fruit-freshness-apk
          path: bin/*.apk