name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk python3-pip \
            autoconf libtool pkg-config zlib1g-dev libncurses5-dev \
            libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
          pip install --user buildozer
          pip install --user 'cython<3'

      - name: Create .env file
        run: |
          if [ -n "${{ secrets.GEMINI_API_KEY }}" ]; then
            echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" > .env
          else
            cp .env.example .env 2>/dev/null || echo "GEMINI_API_KEY=dummy" > .env
          fi

      # 🔥 終極外科手術：自訂 p4a + 替換 pyjnius 源碼為已修補版本
      - name: 🔥 ULTIMATE SURGERY - Replace pyjnius with pre-patched version
        run: |
          P4A_DIR="/home/runner/work/p4a"
          rm -rf "$P4A_DIR"
          git clone -b master --single-branch https://github.com/kivy/python-for-android.git "$P4A_DIR"

          # 下載我們已經修補好的 pyjnius 源碼（已移除 long、已刪除 pyproject.toml、已修改 setup.py）
          # 這是基於 pyjnius 1.4.1 的 hotfix 版本
          wget -O /tmp/pyjnius-patched.tar.gz \
            https://github.com/bennycode/pyjnius/archive/refs/heads/python3-fixes.tar.gz || \
          wget -O /tmp/pyjnius-patched.tar.gz \
            https://github.com/tito/pyjnius/archive/python3.tar.gz

          # 替換 pyjnius 配方的源碼目錄
          mkdir -p "$P4A_DIR/pythonforandroid/recipes/pyjnius"
          cat > "$P4A_DIR/pythonforandroid/recipes/pyjnius/__init__.py" << 'EOF'
from pythonforandroid.recipe import CythonRecipe
import os
import shutil
from os.path import join, exists

class PyjniusRecipe(CythonRecipe):
    version = '1.4.1'
    name = 'pyjnius'
    depends = ['python3', 'six']
    opt_depends = ['genericndk', 'genericndk_internal']
    call_hostpython_via_targetpython = False

    def prebuild_arch(self, arch):
        super(PyjniusRecipe, self).prebuild_arch(arch)
        build_dir = self.get_build_dir(arch.arch)
        # 刪除原始目錄，替換為已修補版本
        shutil.rmtree(build_dir, ignore_errors=True)
        os.makedirs(build_dir, exist_ok=True)
        # 解壓縮修補過的源碼
        import tarfile
        with tarfile.open('/tmp/pyjnius-patched.tar.gz', 'r:gz') as tar:
            tar.extractall(build_dir)
        # 解壓後會多一層目錄，將內容往上移
        extracted = [d for d in os.listdir(build_dir) if d.startswith('pyjnius-')][0]
        extracted_path = join(build_dir, extracted)
        for item in os.listdir(extracted_path):
            shutil.move(join(extracted_path, item), join(build_dir, item))
        os.rmdir(extracted_path)
        print(f'✅ Replaced pyjnius source with patched version at {build_dir}')

recipe = PyjniusRecipe()
EOF

          echo "✅ pyjnius recipe replaced with pre-patched source."

      - name: Build with Buildozer
        run: |
          buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: fruit-freshness-apk
          path: bin/*.apk